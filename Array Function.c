#include <stdio.h>
#include <string.h>

int main() {
    char array[45] = " Determination is the key to success";
    printf("Length is: %d\n", strlen(array));
    printf("Size is: %d\n", sizeof(array));
    return 0;
}
